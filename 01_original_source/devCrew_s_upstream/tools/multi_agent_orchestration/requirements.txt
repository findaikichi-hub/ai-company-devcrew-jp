# Core dependencies for Multi-Agent Orchestration Platform
# Python 3.10+ required

# Workflow Orchestration
prefect==2.20.17
celery[redis]==5.3.4
# redis is included with celery[redis]

# Data Validation and Serialization
pydantic==2.5.3
pyyaml==6.0.1

# CLI
click==8.1.7
tabulate==0.9.0

# Utilities
python-dateutil==2.8.2

# Optional dependencies (for enhanced features)
# PostgreSQL support (for handoff transaction log)
psycopg2-binary==2.9.9  # Optional: only if using PostgreSQL

# RabbitMQ support (alternative message broker)
# kombu==5.3.4  # Uncomment if using RabbitMQ

# Airflow support (legacy orchestration)
# apache-airflow==2.7.3  # Uncomment if using Airflow

# Monitoring
# flower==2.0.1  # Celery monitoring UI
# prometheus-client==0.19.0  # Metrics export

# Testing dependencies
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-celery==0.0.0
pytest-cov==4.1.0
pytest-mock==3.12.0
fakeredis==2.20.1

# Code quality
black==26.3.1
flake8==7.0.0
mypy==1.8.0
isort==5.13.2
pylint==3.0.3
